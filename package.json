{
  "name": "restful-payment-gateway",
  "version": "1.0.0",
  "description": " a basic RESTful API that allows small businesses to accept payments ",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "test": "jest --detectOpenHandles --maxWorkers=1",
    "test:ci": "jest --detectOpenHandles --forceExit --maxWorkers=1 --coverage",
    "start:DEV": "NODE_ENV=development nodemon server.js",
    "start:PROD": "NODE_ENV=production node server.js",
    "build": "echo \"No build needed for plain JS API\""
  },
  "author": "oluwatosin oni",
  "license": "ISC",
  "dependencies": {
    "axios": "^1.11.0",
    "dotenv": "^17.2.1",
    "express": "^5.1.0",
    "morgan": "^1.10.1"
  },
  "devDependencies": {
    "jest": "^30.0.5",
    "supertest": "^7.1.4"
  }
}
